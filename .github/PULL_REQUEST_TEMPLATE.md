<!-- Thanks for contributing! Please complete the checklist below. -->

## Summary

<!-- What does this PR change and why? -->

## Test plan

- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm test` passes
- [ ] If touching server code or schema: `pnpm db:migrate` is idempotent on a fresh DB

## DCO

- [ ] Every commit on this PR is signed off with `Signed-off-by: Your Name <your.email@example.com>` (use `git commit -s` to add it).

By signing off, you certify the changes per the [Developer Certificate of Origin](https://developercertificate.org/). The `DCO` GitHub check enforces this — unsigned commits will block the merge.
