# Wait at least 24h (1440 min) after a version is published before installing it.
# Most supply-chain compromises on npm are detected and yanked within hours, so
# a short cooldown blocks the worst of them without slowing routine upgrades.
minimum-release-age=1440
